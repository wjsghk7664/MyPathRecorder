<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/widgetOrange</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_app_logo</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
        <item name="postSplashScreenTheme">@style/Theme.MyPathRecorder</item>
    </style>

    <!-- Base application theme. -->
    <style name="Base.Theme.MyPathRecorder" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyPathRecorder" parent="Base.Theme.MyPathRecorder" />
</resources>